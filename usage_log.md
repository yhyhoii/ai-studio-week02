### 1. Q: 터미널에 ':'가 뜨고 어떻게 나와야 할까? 
A: 터미널에 'q'를 누르면 나올 수 있습니다.

### 2. Q: shop.py 파일 하나로 어떻게 커밋을 3개나 나눠서 할 수 있어?
A: 커밋은 "파일 단위"가 아니라 "그 순간에 내가 저장한 스냅샷 단위"예요. 같은 공책이라도 페이지를 쓸 때마다 사진을 찍어두면 여러 장이 남는 것처럼, 같은 shop.py를 단계별로(Customer만 → Order 추가 → 검증 코드 추가) 수정하고 그때그때 커밋하면 한 파일이라도 커밋 3개가 쌓여요.

### 3. Q: git push 할 때마다 매번 목적지(origin main)를 알려줘야 할까?
A: 택배기사가 한 번 배송한 집 주소를 이미 외운 상황과 같아요. 처음 한 번은 "origin main"이라는 목적지를 알려줘야(연결·tracking) 하지만, 그 다음부터는 그냥 "가"라고만 해도(git push) 어디로 가야 할지 이미 기억하고 있습니다.

### 4. Q: 실수로 커밋을 해버렸는데 전부 되돌릴 수 있어?
A: 네, 아직 GitHub에 올리기 전(push 전)이라 로컬 커밋만 지우면 안전했어요. `.git` 폴더를 지우고 `git init`을 다시 하면 커밋 이력만 사라지고 파일 내용은 그대로 남아요.